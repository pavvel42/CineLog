// CineLog Configuration Template
// Plik static/js/config.js jest ignorowany przez git i tworzony automatycznie
// przy starcie serwera (run.py) na podstawie tego szablonu. Możesz go też
// skopiować ręcznie i wpisać własne wartości domyślne.

window.CINELOG_CONFIG = {
  // Google OAuth 2.0 Client ID (from Google Cloud Console)
  // Format: "1234567890-abcdefg.apps.googleusercontent.com"
  GOOGLE_CLIENT_ID: "",

  // Klucz TMDb API - używany tylko w trybie bez backendu (GitHub Pages).
  // W trybie z backendem trzymaj klucz w .env (TMDB_API_KEY) albo wpisz go
  // w aplikacji (BYOK - trafia wtedy do localStorage, nie do tego pliku).
  TMDB_API_KEY: ""
};
